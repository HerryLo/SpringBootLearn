<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.DepartmentMapper">

    <!-- 结果映射 -->
    <resultMap id="deptMap" type="com.example.demo.pojo.Department">
        <id property="id" column="id"/>
        <result property="deptName" column="dept_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询所有 -->
<!--    <select id="selectAll" resultMap="deptMap">-->
<!--        SELECT * FROM department ORDER BY id-->
<!--    </select>-->

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="deptMap">
        SELECT * FROM department WHERE id = #{id}
    </select>

    <!-- 模糊查询 -->
    <select id="selectByName" resultMap="deptMap">
        SELECT * FROM department WHERE dept_name LIKE CONCAT('%', #{name}, '%')
    </select>

    <!-- 新增 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department (dept_name, create_time, update_time)
        VALUES (#{deptName}, NOW(), NOW())
    </insert>

    <!-- 修改 -->
    <update id="update">
        UPDATE department
        SET dept_name = #{deptName}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除 -->
    <delete id="deleteById">
        DELETE FROM department WHERE id = #{id}
    </delete>

</mapper>